<p id="notice"><%= notice %></p>
<h1>Listing Users</h1>
<li>Signed in as <%= @user.email %></li>

<%= link_to 'logout', destroy_user_session_path %>

<table>
  <tr>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.email %></td>
    <td><%= link_to 'Show', user %></td>
    <!-- <td><%= link_to 'Edit', edit_bsb_feed_path(user) %></td>
    <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New user', new_user_session_path, data: { keybinding: 'a' }  %>